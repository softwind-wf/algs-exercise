# 部署视频脚本（买家版）

> 用途：卖家录屏演示用，买家照做即可完成部署。建议总时长 8~12 分钟。
> 本文按"画面内容 + 旁白"逐段编写，可直接照着念/照着录。
> 【】内为录屏时的操作提示，不要念出来。

---

## 视频信息

- 标题建议：《大学教务网站源码 部署教程（Docker 一键 / IDEA 本地 双方案）》
- 时长：约 10 分钟
- 结构：开场（1 分钟）→ Docker 部署（3 分钟）→ IDEA 部署（3 分钟）→ 功能演示（2 分钟）→ 常见问题（1 分钟）

---

## 第 0 段：开场（约 1 分钟）

【画面：项目源码文件夹 / 系统首页截图】

**旁白：**
大家好，这份视频教大家怎么把这份《大学教务信息网站》源码跑起来。视频里会演示两种方式：
第一种是 **Docker 一键部署**，最简单，推荐大家用这个；
第二种是 **IDEA 本地运行**，适合想在代码里改东西的同学。
不管哪种方式，跑起来之后你都能用三个演示账号登录：学生 zhang、教师 katz、管理员 admin，密码都是 password。
视频最后会演示系统的主要功能，并解答几个常见问题。我们开始。

---

## 第 1 段：环境准备检查（约 1 分钟）

【画面：依次打开命令提示符，输入命令验证环境】

**旁白：**
先检查环境。按 Win+R，输入 cmd 回车，打开命令行。

【输入 `java -version`，画面切到输出】
**旁白：**
第一条，`java -version`。看到 java version "1.8" 就行。本项目的运行环境是 JDK 8。

【输入 `mvn -version`】
**旁白：**
第二条，`mvn -version`。确认 Maven 已经安装。如果你没有装 Maven，推荐直接用 Docker 方式，可以完全跳过本机 Java 和 Maven。

【输入 `docker --version` 和 `docker compose version`】
**旁白：**
第三条，`docker --version` 和 `docker compose version`。如果你装好了 Docker Desktop，并且已经启动，会看到版本号。**用 Docker 方式的话，只需要这一项就够，不需要装 Java 和 Maven。**

> 💡 旁白补充（可选）：没有 Docker 也没关系，看后面的"方式二：IDEA 本地运行"。

---

## 第 2 段：方式一 Docker 一键部署（约 3 分钟）

【画面：进入源码根目录（能看到 docker-compose.yml 的文件夹）】

**旁白：**
先把源码解压到一个路径里，注意**路径不要有中文和空格**，比如放到 `D:\university-web`。
然后在文件夹地址栏输入 cmd 回车，打开命令行（或者用 IDEA 自带的终端）。

【输入 `docker compose up -d --build`，回车，画面停在构建过程】

**旁白：**
输入这条命令：`docker compose up -d --build`，回车。它会自动做四件事：
第一，拉取 MySQL 8 和 Redis 7 的镜像；
第二，编译打包项目；
第三，启动 MySQL、Redis 和应用三个容器；
第四，应用启动时，Flyway 会自动执行数据库迁移脚本，**自动建表、自动灌入演示数据，不需要你手动导入任何 SQL 文件**。

第一次运行因为要下载依赖和镜像，可能需要 5 到 15 分钟，属于正常现象，耐心等待。

【画面：输入 `docker compose ps`，显示三个容器都 Up】

**旁白：**
等它跑完，输入 `docker compose ps` 查看状态。看到 `university-app`、`university-mysql`、`university-redis` 三个容器都是 Up 状态，就说明部署成功了。

【画面：浏览器打开 http://localhost:8080，显示登录页】

**旁白：**
打开浏览器，输入 `http://localhost:8080`，看到登录页面，就成功了！

【画面：演示登录 admin / password，进入后台】

**旁白：**
输入演示账号：admin，密码 password，点登录，就能进入教务管理后台。

**旁白（收尾）：**
以后要关闭系统，在命令行输入 `docker compose down`；要重新启动，输入 `docker compose up -d` 就行，数据都存在 Docker 卷里，不会丢。

---

## 第 3 段：方式二 IDEA 本地运行（约 3 分钟）

> 本段只对没有 Docker、想改代码的同学；用了 Docker 的可以直接跳到第 4 段。

【画面：IDEA 打开项目，等待 Maven 下载依赖】

**旁白：**
本地运行需要三样东西：JDK 8、Maven、MySQL 8（本机安装版）。项目里的 Redis 是可选项，没有 Redis 也能跑，系统会自动退回单机模式。

【画面：IDEA 设置 Project SDK 为 JDK 8；Maven 设置界面】

**旁白：**
用 IDEA 打开源码文件夹，等右下角 Maven 依赖下载完成。然后把项目的 JDK 设置为 1.8。

【画面：打开 application.yml，指出 datasource 配置】

**旁白：**
打开 `src/main/resources/application.yml`，找到 datasource 配置。默认连接的是本机 MySQL：库名 university，账号 root，密码 root。如果你的 MySQL 账号密码不一样，在这里改成你自己的。

【画面：命令行创建数据库，输入 MySQL 命令】

**旁白：**
在 MySQL 里创建数据库，名字必须是 university，字符集 utf8mb4。可以复制这条命令：
`CREATE DATABASE IF NOT EXISTS university DEFAULT CHARACTER SET utf8mb4;`
建好库就行，**表结构和数据不用管，应用启动时会自动建**。

【画面：点击 IDEA 运行按钮，控制台出现 Started UniversityApplication】

**旁白：**
回到 IDEA，找到 `UniversityApplication` 主类，点运行。看到控制台输出 `Started UniversityApplication`，然后浏览器打开 `http://localhost:8080`，就能访问了。

**旁白（常见补充）：**
如果 8080 端口被占用了，在 `application.yml` 第一行把 `port: 8080` 改成其他端口，比如 8088，重启即可。

---

## 第 4 段：功能演示（约 2 分钟）

【画面：依次切换页面，语速可以快一些】

**旁白（分屏切换）：**
跑起来之后，我们用三个账号各看一眼核心功能。

1. **管理员 admin**：登录后看左侧菜单——院系、课程、开课班、教师、学生、排课这些基础数据管理；还有"在线用户"页面，能看到当前在线的人数和明细，这个统计是 Redis 实时算的；"审计日志"记录所有敏感操作。
2. **学生 zhang**：退出，换 zhang / password 登录。看学生中心的课表、成绩单、导师；然后点"论坛"——可以发帖、回帖、点赞、搜索，帖子支持全文检索；再点"聊天"——这里是亮点，可以按"我的同学 / 我的老师"找联系人，还能按院系浏览老师，点开就能实时私聊。
3. **教师 katz**：换 katz 登录，教师中心能看到授课班和学生名单，也能进聊天和论坛回复学生。

**旁白：**
首页还有公告系统，管理端可以发布分类公告、设置定时发布和过期时间。功能就演示到这里，每个页面都能正常使用。

---

## 第 5 段：常见问题解答（约 1 分钟）

【画面：回到命令行/浏览器，配合说明】

**旁白（逐条，配画面）：**

1. **登录后过一会儿又要重新登录？**
   这是本机同时跑着 IDEA 实例和 Docker 实例导致的——浏览器 Cookie 不区分端口，两个实例互相覆盖登录状态。解决办法：只保留一个实例运行；或者用 Docker 部署的，系统已经自动用了独立的 Cookie 名，不受影响。

2. **8080 端口被占用？**
   Docker 方式：命令行先执行 `$env:APP_PORT=8090`（Windows），再 `docker compose up -d`，然后访问 `http://localhost:8090`。IDEA 方式：改 application.yml 里的 port。

3. **数据库连不上？**
   检查 MySQL 是否启动、账号密码是否和 application.yml 一致、库名是否为 university。

4. **Docker 方式下数据库/头像数据会丢吗？**
   不会。数据存在 Docker 命名卷里，`docker compose down` 不会删除，重新 up 数据还在。

5. **想清空数据重新来一遍？**
   执行 `docker compose down -v`（会删除所有数据卷，慎用），再 `docker compose up -d --build`。

**旁白（结尾）：**
以上就是完整的部署和功能演示。如果还有问题，先看随源码附带的 README 文档，里面有更详细的说明；再解决不了，随时联系我。谢谢观看！

---

## 附录 A：录屏清单（防止漏录）

- [ ] 环境检查三条命令（java / mvn / docker）
- [ ] Docker 方式：compose up -d --build 全过程
- [ ] docker compose ps 三容器 Up
- [ ] 浏览器打开登录页
- [ ] admin 登录后台 + 在线用户页 + 审计日志页
- [ ] zhang 登录：学生中心 + 论坛发帖/搜索 + 聊天找人/发消息
- [ ] katz 登录：教师中心 + 回复学生
- [ ] 公告管理（可选加分项）
- [ ] 常见问题 5 条（可只讲不录）

## 附录 B：脚本中出现的命令速查

```bash
# Docker 部署
docker compose up -d --build      # 首次构建并启动
docker compose ps                 # 查看状态
docker compose down               # 停止（保留数据）
docker compose down -v            # 停止并删除数据（慎用）
$env:APP_PORT=8090                # Windows 换端口（PowerShell）
export APP_PORT=8090              # Linux/macOS 换端口

# MySQL 建库（IDEA 方式）
CREATE DATABASE IF NOT EXISTS university DEFAULT CHARACTER SET utf8mb4;
```

## 附录 C：给买家的文字版精简指引（可随网盘一起发）

> 1. 装 Docker Desktop 并启动；
> 2. 源码解压到无中文路径，如 D:\university-web；
> 3. 在该目录打开命令行，执行 `docker compose up -d --build`；
> 4. 等 5~15 分钟，出现三个 Up 容器；
> 5. 浏览器访问 http://localhost:8080；
> 6. 账号 zhang / katz / admin，密码都是 password。
